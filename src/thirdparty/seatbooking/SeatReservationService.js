


export default class SeatReservationService {
  static async reserveSeats(noOfSeats) {
    // Implementation of the seat reservation service
    console.log(`${noOfSeats} seats reserved`);
    return true;
  }
}

